import React, {PropTypes} from 'react';

class NotAllowed extends React.PureComponent {
    render() {
        return (
            <div>NOT ALLOWED</div>
        );
    }
}
NotAllowed.propTypes = {};
export default NotAllowed;