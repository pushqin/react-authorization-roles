import React, {PropTypes} from 'react';

class HomePage extends React.PureComponent {
    render() {
        return (
            <div>Home Page</div>
        );
    }
}
HomePage.propTypes = {};
export default HomePage;